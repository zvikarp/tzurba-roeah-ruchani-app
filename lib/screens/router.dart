import 'package:flutter/material.dart';
import 'package:roeah_ruchani_app/widgets/core/screen.dart';

class RouterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ScreenWidget(
      child: Center(
        child: Text("Router page"),
      ),
    );
  }
}
